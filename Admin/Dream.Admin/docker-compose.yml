version: '3'
services:
  dream_dataapi_1:
    image: 'dream-admin-pro'
    build:
      context: .
      dockerfile: dockerfile   
    restart: always
    expose:
      - "80"
      - "443"  
    ports:
      - "8031:80"
    volumes:
      - /qin/SiteLogs/dream/admin/8031:/app/logs
    logging:
      options:
        max-size: 50m 
  dream_dataapi_2:
    image: 'dream-admin-pro'
    build:
      context: .
      dockerfile: dockerfile   
    restart: always
    expose:
      - "80"
      - "443"  
    ports:
      - "8032:80"
    volumes:
      - /qin/SiteLogs/dream/admin/8032:/app/logs
    logging:
      options:
        max-size: 50m
